---
# Where the dotfiles repo lives on the target machine.
dotfiles_dir: "{{ ansible_facts.env.HOME }}/dotfiles"

# --- Air-gapped (offline) mode ---
# When true, every download is taken from offline_dir on the CONTROL node
# instead of the internet. Stage the artifacts first by running
# fetch-offline.yml on a machine with internet access. Enabled per-group in
# group_vars/airgapped.yml.
offline: false
offline_dir: "{{ playbook_dir }}/offline"

# Packages bundled as RPMs for offline installs (with their full dependency
# closure, resolved by fetch-offline.yml on a matching RHEL release).
# No curl/wget: Ansible modules download via Python, the shell scripts that
# needed them are retired, and full curl conflicts with the curl-minimal
# package on RHEL 9 minimal installs.
offline_rpm_packages:
  - tar
  - unzip
  - xz
  - git
  - tmux
  - python3-pip
  - python3-devel
  - bat

# Packages bundled as DEBs for offline Ubuntu installs (with their full
# dependency closure, resolved by fetch-offline.yml on a matching Ubuntu
# release). Covers the base packages plus everything the apt tasks install.
# No Homebrew prerequisites: brew cannot be installed offline.
offline_deb_packages:
  - tar
  - unzip
  - xz-utils
  - git
  - tmux
  - thefuck
  - bat
  - fontconfig

# Scripts from cmd/ linked into ~/.local/bin (pomo.bak and test are skipped).
cmd_scripts:
  - goggle_ws
  - newx
  - tail_status
  - tail_toggle
  - toggle_ws

# --- Pinned versions / URLs (carried over from the original shell scripts) ---

# ShellCheck (install/rhel/install-ShellCheck.sh)
shellcheck_version: v0.10.0
shellcheck_url: "https://github.com/koalaman/shellcheck/releases/download/{{ shellcheck_version }}/shellcheck-{{ shellcheck_version }}.linux.x86_64.tar.xz"

# ncdu (install/rhel/install-ncdu)
ncdu_url: "https://dev.yorhel.nl/download/ncdu-2.3-linux-x86_64.tar.gz"

# bat (install/rhel/install-bat) — installed from EPEL matching the host's
# RHEL major version (the original hardcoded an EPEL 10 RPM, which fails on RHEL 9)
epel_release_url: "https://dl.fedoraproject.org/pub/epel/epel-release-latest-{{ ansible_facts['distribution_major_version'] }}.noarch.rpm"

# figurine (install/rhel/install-figurine)
figurine_version: v1.3.0
figurine_url: "https://github.com/arsham/figurine/releases/download/{{ figurine_version }}/figurine_linux_amd64_{{ figurine_version }}.tar.gz"

# tealdeer / tldr (install/rhel/install-tldr) — static musl binary, works on
# any RHEL (the original used a Fedora 43 RPM, incompatible with RHEL 9)
tealdeer_url: "https://github.com/dbrgn/tealdeer/releases/latest/download/tealdeer-linux-x86_64-musl"
tldr_pages_url: "https://github.com/tldr-pages/tldr/releases/latest/download/tldr.zip"

# JetBrainsMono Nerd Font (install/ubuntu/install-fonts)
nerd_font_url: "https://github.com/ryanoasis/nerd-fonts/releases/latest/download/JetBrainsMono.tar.xz"

# Homebrew (install/ubuntu/install-brew)
brew_prefix: /home/linuxbrew/.linuxbrew
brew_install_url: "https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh"

# dysk (install/rhel/install-dysk)
dysk_url: "https://dystroy.org/dysk/download/x86_64-linux/dysk"

# tmux plugins (needed by tmux.conf on both OSes); online installs clone TPM
# and let `prefix + I` fetch the rest, offline installs stage all three.
tpm_repo: "https://github.com/tmux-plugins/tpm"
tmux_plugins:
  - { name: tpm, repo: "https://github.com/tmux-plugins/tpm" }
  - { name: tmux-resurrect, repo: "https://github.com/tmux-plugins/tmux-resurrect" }
  - { name: tmux-continuum, repo: "https://github.com/tmux-plugins/tmux-continuum" }
