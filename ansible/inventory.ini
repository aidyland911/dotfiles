# Default: run against this machine.
localhost ansible_connection=local

# To manage remote machines instead, comment out localhost above and
# list them here, then run the matching playbook:
#
# [rhel]
# rhel-box-1 ansible_host=192.168.1.10 ansible_user=aidyland911
#
# [ubuntu]
# ubuntu-box-1 ansible_host=192.168.1.20 ansible_user=aidyland911
